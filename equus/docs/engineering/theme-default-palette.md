# Default theme palette

**Job:** Hex brief for the **`default`** product theme (`:root`). Live tokens are CSS.  
**Upstream:** n/a  
**Status:** **aligned**  
**Code roots:** `app/globals.css`, `lib/theme/nonCssColors.ts`, `lib/theme/appTheme.ts`

Coding rules: [`../conventions/ui-styling.md`](../conventions/ui-styling.md). Second theme: `html.theme-onyx`. Guests use `default`. Signed-in: `personalDetails.preferredTheme` + `EQUUS_THEME` cookie (`AppThemeSync`).

---

## Shipped

| Role | Hex |
|------|-----|
| Base canvas | `#212226` |
| Surface (header/nav/cards) | `#2a3b42` |
| Forest / structural | `#445a4d` |
| Warm CTA | `#b8520a` |
| Sage / success / links | `#779e7f` |

CTA text white; sage-as-background uses dark text `#212226`. Live tokens: `app/globals.css`.
