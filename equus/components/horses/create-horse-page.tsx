"use client";

/**
 * Auth-gated create-horse page — delegates to client content with skeleton until ready.
 */

import { CreateHorsePageContent } from "@/components/horses/create-horse-page-content.tsx";

export function CreateHorsePage() {
  return <CreateHorsePageContent />;
}
