# Review package: d6cda1a..HEAD
## Commits
552f839 feat(seo): add noindex metadata to private/auth pages
## Files changed
 equus/app/[locale]/breeders/new/page.tsx        | 11 +++++++++++  equus/app/[locale]/coaches/new/page.tsx         | 11 +++++++++++  equus/app/[locale]/confirm-email/page.tsx       | 10 ++++++++++  equus/app/[locale]/farriers/new/page.tsx        | 11 +++++++++++  equus/app/[locale]/forgot-password/page.tsx     | 10 ++++++++++  equus/app/[locale]/groomers/new/page.tsx        | 11 +++++++++++  equus/app/[locale]/horses/new/page.tsx          |  9 +++++++++  equus/app/[locale]/not-allowed/page.tsx         | 10 ++++++++++  equus/app/[locale]/notifications/page.tsx       | 10 ++++++++++  equus/app/[locale]/profile/page.tsx             |  9 +++++++++  equus/app/[locale]/resend-confirmation/page.tsx | 10 ++++++++++  equus/app/[locale]/reset-password/page.tsx      | 10 ++++++++++  equus/app/[locale]/riders/new/page.tsx          | 11 +++++++++++  equus/app/[locale]/riding-clubs/new/page.tsx    | 11 +++++++++++  equus/app/[locale]/signin/page.tsx              | 10 ++++++++++  equus/app/[locale]/signup/page.tsx              | 10 ++++++++++  equus/app/[locale]/stables/new/page.tsx         | 10 ++++++++++  equus/app/[locale]/trainers/new/page.tsx        | 11 +++++++++++  equus/app/[locale]/transport/new/page.tsx       | 11 +++++++++++  equus/app/[locale]/veterinaries/new/page.tsx    | 11 +++++++++++  20 files changed, 207 insertions(+)
## Diff
diff --git a/equus/app/[locale]/breeders/new/page.tsx b/equus/app/[locale]/breeders/new/page.tsx index a4e1da9..3973009 100644 --- a/equus/app/[locale]/breeders/new/page.tsx +++ b/equus/app/[locale]/breeders/new/page.tsx @@ -1,4 +1,15 @@ +import type { Metadata } from "next"; +import { generatePrivateMetadata } from "@/lib/seo/metadata-factory.ts"; +  import { EntityPageContent } from "@/components/layout/entity-page-content.tsx"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePrivateMetadata(locale, "/breeders/new", "metadata.breeders"); +} +  export default function CreatePage() {    return <EntityPageContent entity="breeders" showSignIn={false} />;  } \ No newline at end of file diff --git a/equus/app/[locale]/coaches/new/page.tsx b/equus/app/[locale]/coaches/new/page.tsx index 8630b55..e3c23b5 100644 --- a/equus/app/[locale]/coaches/new/page.tsx +++ b/equus/app/[locale]/coaches/new/page.tsx @@ -1,4 +1,15 @@ +import type { Metadata } from "next"; +import { generatePrivateMetadata } from "@/lib/seo/metadata-factory.ts"; +  import { EntityPageContent } from "@/components/layout/entity-page-content.tsx"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePrivateMetadata(locale, "/coaches/new", "metadata.coaches"); +} +  export default function CreatePage() {    return <EntityPageContent entity="coaches" showSignIn={false} />;  } \ No newline at end of file diff --git a/equus/app/[locale]/confirm-email/page.tsx b/equus/app/[locale]/confirm-email/page.tsx index 2be4665..9bdd53a 100644 --- a/equus/app/[locale]/confirm-email/page.tsx +++ b/equus/app/[locale]/confirm-email/page.tsx @@ -4,20 +4,30 @@ import { useTranslations } from "next-intl";  import { Suspense, useEffect, useRef, useState } from "react";  import { useSearchParams } from "next/navigation";    import { AuthPageShell } from "@/components/auth/auth-page-shell.tsx";  import { Alert, AlertDescription } from "@/components/ui/alert";  import { buttonVariants } from "@/components/ui/button";  import { Link } from "@/i18n/navigation.ts";  import { confirmEmail } from "@/lib/api/auth/credentials";  import { cn } from "@/lib/utils";   +import type { Metadata } from "next"; +import { generatePrivateMetadata } from "@/lib/seo/metadata-factory.ts"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePrivateMetadata(locale, "/confirm-email", "metadata.confirmEmail"); +} +  type FlowState = "loading" | "success" | "error" | "missing";    function ConfirmEmailContent() {    const searchParams = useSearchParams();    const t = useTranslations("auth.confirmEmail");    const tCommon = useTranslations("common");    const token = searchParams.get("token");    const [state, setState] = useState<FlowState>(token ? "loading" : "missing");    const [message, setMessage] = useState<string | null>(null);    const submittedRef = useRef(false); diff --git a/equus/app/[locale]/farriers/new/page.tsx b/equus/app/[locale]/farriers/new/page.tsx index 33cf97c..69549f7 100644 --- a/equus/app/[locale]/farriers/new/page.tsx +++ b/equus/app/[locale]/farriers/new/page.tsx @@ -1,4 +1,15 @@ +import type { Metadata } from "next"; +import { generatePrivateMetadata } from "@/lib/seo/metadata-factory.ts"; +  import { EntityPageContent } from "@/components/layout/entity-page-content.tsx"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePrivateMetadata(locale, "/farriers/new", "metadata.farriers"); +} +  export default function CreatePage() {    return <EntityPageContent entity="farriers" showSignIn={false} />;  } \ No newline at end of file diff --git a/equus/app/[locale]/forgot-password/page.tsx b/equus/app/[locale]/forgot-password/page.tsx index 53ef370..9f16277 100644 --- a/equus/app/[locale]/forgot-password/page.tsx +++ b/equus/app/[locale]/forgot-password/page.tsx @@ -11,20 +11,30 @@ import { Alert, AlertDescription } from "@/components/ui/alert";  import { Button } from "@/components/ui/button";  import { FieldGroup } from "@/components/ui/field";  import { Link } from "@/i18n/navigation.ts";  import { requestPasswordReset } from "@/lib/api/auth/credentials";  import {    authFormMessagesFromTranslations,    createAuthFormSchemas,    type ForgotPasswordFormValues,  } from "@/lib/validations/authForms.ts";   +import type { Metadata } from "next"; +import { generatePrivateMetadata } from "@/lib/seo/metadata-factory.ts"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePrivateMetadata(locale, "/forgot-password", "metadata.forgotPassword"); +} +  export default function ForgotPasswordPage() {    const t = useTranslations("auth.forgotPassword");    const tCommon = useTranslations("common");    const tStatus = useTranslations("status");    const tValidation = useTranslations("validation");    const [apiError, setApiError] = useState<string | null>(null);    const [submitted, setSubmitted] = useState(false);      const { forgotPasswordFormSchema } = useMemo(      () => diff --git a/equus/app/[locale]/groomers/new/page.tsx b/equus/app/[locale]/groomers/new/page.tsx index 23b7a4d..39af44c 100644 --- a/equus/app/[locale]/groomers/new/page.tsx +++ b/equus/app/[locale]/groomers/new/page.tsx @@ -1,4 +1,15 @@ +import type { Metadata } from "next"; +import { generatePrivateMetadata } from "@/lib/seo/metadata-factory.ts"; +  import { EntityPageContent } from "@/components/layout/entity-page-content.tsx"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePrivateMetadata(locale, "/groomers/new", "metadata.groomers"); +} +  export default function CreatePage() {    return <EntityPageContent entity="groomers" showSignIn={false} />;  } \ No newline at end of file diff --git a/equus/app/[locale]/horses/new/page.tsx b/equus/app/[locale]/horses/new/page.tsx index eab5ae2..dd76e7c 100644 --- a/equus/app/[locale]/horses/new/page.tsx +++ b/equus/app/[locale]/horses/new/page.tsx @@ -1,14 +1,23 @@ +import type { Metadata } from "next";  import dynamic from "next/dynamic";   +import { generatePrivateMetadata } from "@/lib/seo/metadata-factory.ts";  import { CreateHorsePageSkeleton } from "@/components/horses/create-horse-page-skeleton.tsx";   +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePrivateMetadata(locale, "/horses/new", "metadata.horses"); +} +  const CreateHorsePage = dynamic(    () =>      import("@/components/horses/create-horse-page.tsx").then((m) => ({        default: m.CreateHorsePage,      })),    { loading: () => <CreateHorsePageSkeleton /> },  );    export default function Page() {    return <CreateHorsePage />; diff --git a/equus/app/[locale]/not-allowed/page.tsx b/equus/app/[locale]/not-allowed/page.tsx index 2b7ef7e..76dedcd 100644 --- a/equus/app/[locale]/not-allowed/page.tsx +++ b/equus/app/[locale]/not-allowed/page.tsx @@ -1,19 +1,29 @@  "use client";    import { useTranslations } from "next-intl";  import { Suspense } from "react";  import { useSearchParams } from "next/navigation";    import { AuthAwareStatusActions } from "@/components/status/auth-aware-status-actions.tsx";  import { StatusPageShell } from "@/components/status/status-page-shell.tsx";   +import type { Metadata } from "next"; +import { generatePrivateMetadata } from "@/lib/seo/metadata-factory.ts"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePrivateMetadata(locale, "/not-allowed", "metadata.notAllowed"); +} +  type NotAllowedReason = "forbidden" | "wrong_account" | "email_unverified";    function NotAllowedContent() {    const searchParams = useSearchParams();    const t = useTranslations("status.notAllowed");    const reason = searchParams.get("reason") as NotAllowedReason | null;      const description =      reason === "wrong_account"        ? t("wrongAccount") diff --git a/equus/app/[locale]/notifications/page.tsx b/equus/app/[locale]/notifications/page.tsx index 0dc4b4e..81b104f 100644 --- a/equus/app/[locale]/notifications/page.tsx +++ b/equus/app/[locale]/notifications/page.tsx @@ -1,5 +1,15 @@ +import type { Metadata } from "next"; +import { generatePrivateMetadata } from "@/lib/seo/metadata-factory.ts"; +  import { NotificationsPlaceholderPage } from "@/components/layout/notifications-placeholder-page.tsx";   +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePrivateMetadata(locale, "/notifications", "metadata.notifications"); +} +  export default function NotificationsPage() {    return <NotificationsPlaceholderPage />;  } diff --git a/equus/app/[locale]/profile/page.tsx b/equus/app/[locale]/profile/page.tsx index 9ae9691..cc4c2a4 100644 --- a/equus/app/[locale]/profile/page.tsx +++ b/equus/app/[locale]/profile/page.tsx @@ -1,14 +1,23 @@ +import type { Metadata } from "next";  import dynamic from "next/dynamic";   +import { generatePrivateMetadata } from "@/lib/seo/metadata-factory.ts";  import { ProfilePageSkeleton } from "@/components/profile/profile-page-skeleton.tsx";   +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePrivateMetadata(locale, "/profile", "metadata.profile"); +} +  const ProfilePage = dynamic(    () =>      import("@/components/profile/profile-page.tsx").then((m) => ({        default: m.ProfilePage,      })),    { loading: () => <ProfilePageSkeleton /> },  );    export default function Page() {    return <ProfilePage />; diff --git a/equus/app/[locale]/resend-confirmation/page.tsx b/equus/app/[locale]/resend-confirmation/page.tsx index 62e9de9..792a36b 100644 --- a/equus/app/[locale]/resend-confirmation/page.tsx +++ b/equus/app/[locale]/resend-confirmation/page.tsx @@ -13,20 +13,30 @@ import { FieldGroup } from "@/components/ui/field";  import { Link } from "@/i18n/navigation.ts";  import { requestEmailConfirmation } from "@/lib/api/auth/credentials";  import { isApiClientError } from "@/lib/api/auth/session";  import { cn } from "@/lib/utils";  import {    authFormMessagesFromTranslations,    createAuthFormSchemas,    type ResendConfirmationFormValues,  } from "@/lib/validations/authForms.ts";   +import type { Metadata } from "next"; +import { generatePrivateMetadata } from "@/lib/seo/metadata-factory.ts"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePrivateMetadata(locale, "/resend-confirmation", "metadata.resendConfirmation"); +} +  export default function ResendConfirmationPage() {    const t = useTranslations("auth.resendConfirmation");    const tCommon = useTranslations("common");    const tStatus = useTranslations("status");    const tValidation = useTranslations("validation");    const [apiError, setApiError] = useState<string | null>(null);    const [submitted, setSubmitted] = useState(false);    const [alreadyVerified, setAlreadyVerified] = useState(false);      const { resendConfirmationFormSchema } = useMemo( diff --git a/equus/app/[locale]/reset-password/page.tsx b/equus/app/[locale]/reset-password/page.tsx index 1099c1b..fadbb34 100644 --- a/equus/app/[locale]/reset-password/page.tsx +++ b/equus/app/[locale]/reset-password/page.tsx @@ -14,20 +14,30 @@ import { FieldGroup } from "@/components/ui/field";  import { Link, useRouter } from "@/i18n/navigation.ts";  import { resetPassword } from "@/lib/api/auth/credentials";  import { clearClientAuthSession } from "@/lib/auth/clearClientAuthSession.ts";  import { cn } from "@/lib/utils";  import {    authFormMessagesFromTranslations,    createAuthFormSchemas,    type ResetPasswordFormValues,  } from "@/lib/validations/authForms.ts";   +import type { Metadata } from "next"; +import { generatePrivateMetadata } from "@/lib/seo/metadata-factory.ts"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePrivateMetadata(locale, "/reset-password", "metadata.resetPassword"); +} +  function ResetPasswordContent() {    const router = useRouter();    const searchParams = useSearchParams();    const t = useTranslations("auth.resetPassword");    const tForgot = useTranslations("auth.forgotPassword");    const tCommon = useTranslations("common");    const tAuth = useTranslations("auth");    const tStatus = useTranslations("status");    const tValidation = useTranslations("validation");    const token = searchParams.get("token"); diff --git a/equus/app/[locale]/riders/new/page.tsx b/equus/app/[locale]/riders/new/page.tsx index 522edf2..7952397 100644 --- a/equus/app/[locale]/riders/new/page.tsx +++ b/equus/app/[locale]/riders/new/page.tsx @@ -1,4 +1,15 @@ +import type { Metadata } from "next"; +import { generatePrivateMetadata } from "@/lib/seo/metadata-factory.ts"; +  import { EntityPageContent } from "@/components/layout/entity-page-content.tsx"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePrivateMetadata(locale, "/riders/new", "metadata.riders"); +} +  export default function CreatePage() {    return <EntityPageContent entity="riders" showSignIn={false} />;  } \ No newline at end of file diff --git a/equus/app/[locale]/riding-clubs/new/page.tsx b/equus/app/[locale]/riding-clubs/new/page.tsx index b2f7604..01e5077 100644 --- a/equus/app/[locale]/riding-clubs/new/page.tsx +++ b/equus/app/[locale]/riding-clubs/new/page.tsx @@ -1,4 +1,15 @@ +import type { Metadata } from "next"; +import { generatePrivateMetadata } from "@/lib/seo/metadata-factory.ts"; +  import { EntityPageContent } from "@/components/layout/entity-page-content.tsx"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePrivateMetadata(locale, "/riding-clubs/new", "metadata.ridingClubs"); +} +  export default function CreatePage() {    return <EntityPageContent entity="riding-clubs" showSignIn={false} />;  } \ No newline at end of file diff --git a/equus/app/[locale]/signin/page.tsx b/equus/app/[locale]/signin/page.tsx index 5a8399a..2bc10d9 100644 --- a/equus/app/[locale]/signin/page.tsx +++ b/equus/app/[locale]/signin/page.tsx @@ -17,20 +17,30 @@ import { Link, usePathname, useRouter } from "@/i18n/navigation.ts";  import { normalizeLocale } from "@/i18n/resolveLocale.ts";  import { loginWithCredentials } from "@/lib/api/auth/credentials";  import { isApiClientError } from "@/lib/api/auth/session";  import { resolvePostAuthPath } from "@/lib/navigation/postAuthRedirect.ts";  import {    authFormMessagesFromTranslations,    createAuthFormSchemas,    type SignInFormValues,  } from "@/lib/validations/authForms.ts";   +import type { Metadata } from "next"; +import { generatePrivateMetadata } from "@/lib/seo/metadata-factory.ts"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePrivateMetadata(locale, "/signin", "metadata.signin"); +} +  function SignInContent() {    const router = useRouter();    const pathname = usePathname();    const searchParams = useSearchParams();    const locale = useLocale();    const t = useTranslations("auth.signIn");    const tCommon = useTranslations("common");    const tValidation = useTranslations("validation");    const tErrors = useTranslations("errors");    const [apiError, setApiError] = useState<string | null>(null); diff --git a/equus/app/[locale]/signup/page.tsx b/equus/app/[locale]/signup/page.tsx index b030572..af4ad5b 100644 --- a/equus/app/[locale]/signup/page.tsx +++ b/equus/app/[locale]/signup/page.tsx @@ -17,20 +17,30 @@ import { Link, useRouter } from "@/i18n/navigation.ts";  import { registerWithCredentials } from "@/lib/api/auth/credentials";  import { resolvePostAuthPath } from "@/lib/navigation/postAuthRedirect.ts";  import { isStaffMembershipRef } from "@/lib/utils/inviteRef.ts";  import {    authFormMessagesFromTranslations,    createAuthFormSchemas,    type SignUpFormValues,  } from "@/lib/validations/authForms.ts";  import { useInvitePreview } from "@/hooks/queries/useInvite";   +import type { Metadata } from "next"; +import { generatePrivateMetadata } from "@/lib/seo/metadata-factory.ts"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePrivateMetadata(locale, "/signup", "metadata.signup"); +} +  function SignUpContent() {    const router = useRouter();    const searchParams = useSearchParams();    const t = useTranslations("auth.signUp");    const tCommon = useTranslations("common");    const tInvites = useTranslations("invites.signup");    const tAuth = useTranslations("auth");    const tValidation = useTranslations("validation");    const ref = searchParams.get("ref")?.trim() ?? "";    const isStaffRef = ref ? isStaffMembershipRef(ref) : false; diff --git a/equus/app/[locale]/stables/new/page.tsx b/equus/app/[locale]/stables/new/page.tsx index 63b7bd9..e7e66ab 100644 --- a/equus/app/[locale]/stables/new/page.tsx +++ b/equus/app/[locale]/stables/new/page.tsx @@ -1,10 +1,20 @@ +import type { Metadata } from "next";  import { Suspense } from "react"; + +import { generatePrivateMetadata } from "@/lib/seo/metadata-factory.ts";  import { EntityPageContent } from "@/components/layout/entity-page-content.tsx";   +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePrivateMetadata(locale, "/stables/new", "metadata.stables"); +} +  export default function CreateStablePage() {    return (      <Suspense>        <EntityPageContent entity="stables" showSignIn={false} />      </Suspense>    );  } diff --git a/equus/app/[locale]/trainers/new/page.tsx b/equus/app/[locale]/trainers/new/page.tsx index 29c4d33..5c68fe7 100644 --- a/equus/app/[locale]/trainers/new/page.tsx +++ b/equus/app/[locale]/trainers/new/page.tsx @@ -1,4 +1,15 @@ +import type { Metadata } from "next"; +import { generatePrivateMetadata } from "@/lib/seo/metadata-factory.ts"; +  import { EntityPageContent } from "@/components/layout/entity-page-content.tsx"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePrivateMetadata(locale, "/trainers/new", "metadata.trainers"); +} +  export default function CreatePage() {    return <EntityPageContent entity="trainers" showSignIn={false} />;  } \ No newline at end of file diff --git a/equus/app/[locale]/transport/new/page.tsx b/equus/app/[locale]/transport/new/page.tsx index d9ec63c..4e40cfc 100644 --- a/equus/app/[locale]/transport/new/page.tsx +++ b/equus/app/[locale]/transport/new/page.tsx @@ -1,4 +1,15 @@ +import type { Metadata } from "next"; +import { generatePrivateMetadata } from "@/lib/seo/metadata-factory.ts"; +  import { EntityPageContent } from "@/components/layout/entity-page-content.tsx"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePrivateMetadata(locale, "/transport/new", "metadata.transport"); +} +  export default function CreatePage() {    return <EntityPageContent entity="transport" showSignIn={false} />;  } \ No newline at end of file diff --git a/equus/app/[locale]/veterinaries/new/page.tsx b/equus/app/[locale]/veterinaries/new/page.tsx index ee91a85..505adb7 100644 --- a/equus/app/[locale]/veterinaries/new/page.tsx +++ b/equus/app/[locale]/veterinaries/new/page.tsx @@ -1,4 +1,15 @@ +import type { Metadata } from "next"; +import { generatePrivateMetadata } from "@/lib/seo/metadata-factory.ts"; +  import { EntityPageContent } from "@/components/layout/entity-page-content.tsx"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePrivateMetadata(locale, "/veterinaries/new", "metadata.veterinaries"); +} +  export default function CreatePage() {    return <EntityPageContent entity="veterinaries" showSignIn={false} />;  } \ No newline at end of file
