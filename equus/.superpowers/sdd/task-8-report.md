# Task 8: Create `lib/seo/entity-metadata.ts` — Complete

- Created `lib/seo/entity-metadata.ts` with `generateHorseMetadata`, `generateStableMetadata`, `generateProviderMetadata`, and `generateUserMetadata`, all sharing a `buildEntityMetadata` helper.
- Typecheck (`npx tsc --noEmit`) passes — no errors from new file (pre-existing test errors unchanged).
- Committed: `e4710c4` feat(seo): add entity-specific metadata builders
