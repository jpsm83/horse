# Review package: e485bcc..HEAD
## Commits
d6cda1a feat(seo): add metadata to all static public pages
## Files changed
 equus/app/[locale]/breeders/page.tsx            | 10 ++++++++++  equus/app/[locale]/coaches/page.tsx             | 10 ++++++++++  equus/app/[locale]/farriers/page.tsx            | 10 ++++++++++  equus/app/[locale]/groomers/page.tsx            | 10 ++++++++++  equus/app/[locale]/home/page.tsx                | 10 ++++++++++  equus/app/[locale]/horses/page.tsx              | 10 ++++++++++  equus/app/[locale]/ownership-transfers/page.tsx |  9 +++++++++  equus/app/[locale]/page.tsx                     |  9 +++++++++  equus/app/[locale]/relationships/page.tsx       |  9 +++++++++  equus/app/[locale]/riders/page.tsx              | 10 ++++++++++  equus/app/[locale]/riding-clubs/page.tsx        | 10 ++++++++++  equus/app/[locale]/stables/page.tsx             | 10 ++++++++++  equus/app/[locale]/trainers/page.tsx            | 10 ++++++++++  equus/app/[locale]/transport/page.tsx           | 10 ++++++++++  equus/app/[locale]/veterinaries/page.tsx        | 10 ++++++++++  equus/app/[locale]/workplaces/page.tsx          |  9 +++++++++  16 files changed, 156 insertions(+)
## Diff
diff --git a/equus/app/[locale]/breeders/page.tsx b/equus/app/[locale]/breeders/page.tsx index 2c25229..5163840 100644 --- a/equus/app/[locale]/breeders/page.tsx +++ b/equus/app/[locale]/breeders/page.tsx @@ -1,4 +1,14 @@  import { EntityPageContent } from "@/components/layout/entity-page-content.tsx"; +import type { Metadata } from "next"; +import { generatePublicMetadata } from "@/lib/seo/metadata-factory.ts"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePublicMetadata(locale, "/breeders", "metadata.breeders"); +} +  export default function EntityPage() {    return <EntityPageContent entity="breeders" owned={true} />;  } \ No newline at end of file diff --git a/equus/app/[locale]/coaches/page.tsx b/equus/app/[locale]/coaches/page.tsx index 7764639..ab25cbe 100644 --- a/equus/app/[locale]/coaches/page.tsx +++ b/equus/app/[locale]/coaches/page.tsx @@ -1,4 +1,14 @@  import { EntityPageContent } from "@/components/layout/entity-page-content.tsx"; +import type { Metadata } from "next"; +import { generatePublicMetadata } from "@/lib/seo/metadata-factory.ts"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePublicMetadata(locale, "/coaches", "metadata.coaches"); +} +  export default function EntityPage() {    return <EntityPageContent entity="coaches" owned={true} />;  } \ No newline at end of file diff --git a/equus/app/[locale]/farriers/page.tsx b/equus/app/[locale]/farriers/page.tsx index 836ccdc..237c23f 100644 --- a/equus/app/[locale]/farriers/page.tsx +++ b/equus/app/[locale]/farriers/page.tsx @@ -1,4 +1,14 @@  import { EntityPageContent } from "@/components/layout/entity-page-content.tsx"; +import type { Metadata } from "next"; +import { generatePublicMetadata } from "@/lib/seo/metadata-factory.ts"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePublicMetadata(locale, "/farriers", "metadata.farriers"); +} +  export default function EntityPage() {    return <EntityPageContent entity="farriers" owned={true} />;  } \ No newline at end of file diff --git a/equus/app/[locale]/groomers/page.tsx b/equus/app/[locale]/groomers/page.tsx index 4461677..d9398cb 100644 --- a/equus/app/[locale]/groomers/page.tsx +++ b/equus/app/[locale]/groomers/page.tsx @@ -1,4 +1,14 @@  import { EntityPageContent } from "@/components/layout/entity-page-content.tsx"; +import type { Metadata } from "next"; +import { generatePublicMetadata } from "@/lib/seo/metadata-factory.ts"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePublicMetadata(locale, "/groomers", "metadata.groomers"); +} +  export default function EntityPage() {    return <EntityPageContent entity="groomers" owned={true} />;  } \ No newline at end of file diff --git a/equus/app/[locale]/home/page.tsx b/equus/app/[locale]/home/page.tsx index 64534e8..7737493 100644 --- a/equus/app/[locale]/home/page.tsx +++ b/equus/app/[locale]/home/page.tsx @@ -1,14 +1,24 @@  import dynamic from "next/dynamic";    import { UserHomePageSkeleton } from "@/components/home/user-home-page-skeleton.tsx";   +import type { Metadata } from "next"; +import { generatePublicMetadata } from "@/lib/seo/metadata-factory.ts"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePublicMetadata(locale, "/home", "metadata.homeDashboard"); +} +  const UserHomePage = dynamic(    () =>      import("@/components/home/user-home-page.tsx").then((m) => ({        default: m.UserHomePage,      })),    { loading: () => <UserHomePageSkeleton /> },  );    export default function Page() {    return <UserHomePage />; diff --git a/equus/app/[locale]/horses/page.tsx b/equus/app/[locale]/horses/page.tsx index 998f47a..a036ef6 100644 --- a/equus/app/[locale]/horses/page.tsx +++ b/equus/app/[locale]/horses/page.tsx @@ -1,14 +1,24 @@  import dynamic from "next/dynamic";    import { HorseListPageSkeleton } from "@/components/horses/horse-list-page-skeleton.tsx";   +import type { Metadata } from "next"; +import { generatePublicMetadata } from "@/lib/seo/metadata-factory.ts"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePublicMetadata(locale, "/horses", "metadata.horses"); +} +  const HorseListPage = dynamic(    () =>      import("@/components/horses/horse-list-page.tsx").then((m) => ({        default: m.HorseListPage,      })),    { loading: () => <HorseListPageSkeleton /> },  );    export default function Page() {    return <HorseListPage />; diff --git a/equus/app/[locale]/ownership-transfers/page.tsx b/equus/app/[locale]/ownership-transfers/page.tsx index a9a46ff..05f1f5d 100644 --- a/equus/app/[locale]/ownership-transfers/page.tsx +++ b/equus/app/[locale]/ownership-transfers/page.tsx @@ -12,20 +12,29 @@ import { AppHomeLink } from "@/components/navigation/app-home-link.tsx";  import { useRouter } from "@/i18n/navigation.ts";  import {    useAcceptOwnershipTransfer,    useDeclineOwnershipTransfer,  } from "@/hooks/queries/useOwnershipTransfer.ts";  import { useAppAuth } from "@/hooks/use-app-auth";  import { usePendingOwnershipTransfers } from "@/hooks/queries/useAuthData";  import { isApiClientError } from "@/lib/api/auth/session";  import { buildSignInPath } from "@/lib/navigation/postAuthRedirect.ts";  import { cn } from "@/lib/utils"; +import type { Metadata } from "next"; +import { generatePublicMetadata } from "@/lib/seo/metadata-factory.ts"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePublicMetadata(locale, "/ownership-transfers", "metadata.ownershipTransfers"); +}    function OwnershipTransfersLoadingShell() {    return <InviteHubPageSkeleton titleNamespace="invites.ownershipTransfers" />;  }    function OwnershipTransfersContent() {    const router = useRouter();    const searchParams = useSearchParams();    const t = useTranslations("invites.ownershipTransfers");    const tCommon = useTranslations("common"); diff --git a/equus/app/[locale]/page.tsx b/equus/app/[locale]/page.tsx index 44ab8bb..47af107 100644 --- a/equus/app/[locale]/page.tsx +++ b/equus/app/[locale]/page.tsx @@ -1,12 +1,21 @@  import { Suspense } from "react";    import { HomePage } from "@/components/home/home-page.tsx";  import { HomePageSkeleton } from "@/components/home/home-page-skeleton.tsx"; +import type { Metadata } from "next"; +import { generatePublicMetadata } from "@/lib/seo/metadata-factory.ts"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePublicMetadata(locale, "", "metadata.home"); +}    export default function Page() {    return (      <Suspense fallback={<HomePageSkeleton />}>        <HomePage />      </Suspense>    );  } diff --git a/equus/app/[locale]/relationships/page.tsx b/equus/app/[locale]/relationships/page.tsx index bd8f9f3..7ea9554 100644 --- a/equus/app/[locale]/relationships/page.tsx +++ b/equus/app/[locale]/relationships/page.tsx @@ -12,20 +12,29 @@ import { AppHomeLink } from "@/components/navigation/app-home-link.tsx";  import { useRouter } from "@/i18n/navigation.ts";  import {    useAcceptRelationship,    useDeclineRelationship,  } from "@/hooks/queries/useRelationship.ts";  import { useAppAuth } from "@/hooks/use-app-auth";  import { usePendingRelationships } from "@/hooks/queries/useAuthData";  import { isApiClientError } from "@/lib/api/auth/session";  import { buildSignInPath } from "@/lib/navigation/postAuthRedirect.ts";  import { cn } from "@/lib/utils"; +import type { Metadata } from "next"; +import { generatePublicMetadata } from "@/lib/seo/metadata-factory.ts"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePublicMetadata(locale, "/relationships", "metadata.relationships"); +}    function RelationshipsLoadingShell() {    return <InviteHubPageSkeleton titleNamespace="invites.relationships" />;  }    function RelationshipsContent() {    const router = useRouter();    const searchParams = useSearchParams();    const t = useTranslations("invites.relationships");    const tCommon = useTranslations("common"); diff --git a/equus/app/[locale]/riders/page.tsx b/equus/app/[locale]/riders/page.tsx index 88a69e9..01ea851 100644 --- a/equus/app/[locale]/riders/page.tsx +++ b/equus/app/[locale]/riders/page.tsx @@ -1,4 +1,14 @@  import { EntityPageContent } from "@/components/layout/entity-page-content.tsx"; +import type { Metadata } from "next"; +import { generatePublicMetadata } from "@/lib/seo/metadata-factory.ts"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePublicMetadata(locale, "/riders", "metadata.riders"); +} +  export default function EntityPage() {    return <EntityPageContent entity="riders" owned={true} />;  } \ No newline at end of file diff --git a/equus/app/[locale]/riding-clubs/page.tsx b/equus/app/[locale]/riding-clubs/page.tsx index 9d87d9b..cdd78cf 100644 --- a/equus/app/[locale]/riding-clubs/page.tsx +++ b/equus/app/[locale]/riding-clubs/page.tsx @@ -1,4 +1,14 @@  import { EntityPageContent } from "@/components/layout/entity-page-content.tsx"; +import type { Metadata } from "next"; +import { generatePublicMetadata } from "@/lib/seo/metadata-factory.ts"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePublicMetadata(locale, "/riding-clubs", "metadata.ridingClubs"); +} +  export default function EntityPage() {    return <EntityPageContent entity="riding-clubs" owned={true} />;  } \ No newline at end of file diff --git a/equus/app/[locale]/stables/page.tsx b/equus/app/[locale]/stables/page.tsx index 5e3988e..ec047a7 100644 --- a/equus/app/[locale]/stables/page.tsx +++ b/equus/app/[locale]/stables/page.tsx @@ -1,4 +1,14 @@  import { EntityPageContent } from "@/components/layout/entity-page-content.tsx"; +import type { Metadata } from "next"; +import { generatePublicMetadata } from "@/lib/seo/metadata-factory.ts"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePublicMetadata(locale, "/stables", "metadata.stables"); +} +  export default function EntityPage() {    return <EntityPageContent entity="stables" owned={true} />;  } \ No newline at end of file diff --git a/equus/app/[locale]/trainers/page.tsx b/equus/app/[locale]/trainers/page.tsx index 0572f5c..7c6f8f9 100644 --- a/equus/app/[locale]/trainers/page.tsx +++ b/equus/app/[locale]/trainers/page.tsx @@ -1,4 +1,14 @@  import { EntityPageContent } from "@/components/layout/entity-page-content.tsx"; +import type { Metadata } from "next"; +import { generatePublicMetadata } from "@/lib/seo/metadata-factory.ts"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePublicMetadata(locale, "/trainers", "metadata.trainers"); +} +  export default function EntityPage() {    return <EntityPageContent entity="trainers" owned={true} />;  } \ No newline at end of file diff --git a/equus/app/[locale]/transport/page.tsx b/equus/app/[locale]/transport/page.tsx index 304173b..82f001e 100644 --- a/equus/app/[locale]/transport/page.tsx +++ b/equus/app/[locale]/transport/page.tsx @@ -1,4 +1,14 @@  import { EntityPageContent } from "@/components/layout/entity-page-content.tsx"; +import type { Metadata } from "next"; +import { generatePublicMetadata } from "@/lib/seo/metadata-factory.ts"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePublicMetadata(locale, "/transport", "metadata.transport"); +} +  export default function EntityPage() {    return <EntityPageContent entity="transport" owned={true} />;  } \ No newline at end of file diff --git a/equus/app/[locale]/veterinaries/page.tsx b/equus/app/[locale]/veterinaries/page.tsx index 91007bd..5100320 100644 --- a/equus/app/[locale]/veterinaries/page.tsx +++ b/equus/app/[locale]/veterinaries/page.tsx @@ -1,4 +1,14 @@  import { EntityPageContent } from "@/components/layout/entity-page-content.tsx"; +import type { Metadata } from "next"; +import { generatePublicMetadata } from "@/lib/seo/metadata-factory.ts"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePublicMetadata(locale, "/veterinaries", "metadata.veterinaries"); +} +  export default function EntityPage() {    return <EntityPageContent entity="veterinaries" owned={true} />;  } \ No newline at end of file diff --git a/equus/app/[locale]/workplaces/page.tsx b/equus/app/[locale]/workplaces/page.tsx index caba67e..32196a3 100644 --- a/equus/app/[locale]/workplaces/page.tsx +++ b/equus/app/[locale]/workplaces/page.tsx @@ -12,20 +12,29 @@ import { AppHomeLink } from "@/components/navigation/app-home-link.tsx";  import { useRouter } from "@/i18n/navigation.ts";  import {    useAcceptWorkplaceInvitation,    useDeclineWorkplaceInvitation,    useWorkplaces,  } from "@/hooks/queries/useAuthData";  import { useAppAuth } from "@/hooks/use-app-auth";  import { isApiClientError } from "@/lib/api/auth/session";  import { buildSignInPath } from "@/lib/navigation/postAuthRedirect.ts";  import { cn } from "@/lib/utils"; +import type { Metadata } from "next"; +import { generatePublicMetadata } from "@/lib/seo/metadata-factory.ts"; + +type PageProps = { params: Promise<{ locale: string }> }; + +export async function generateMetadata({ params }: PageProps): Promise<Metadata> { +  const { locale } = await params; +  return generatePublicMetadata(locale, "/workplaces", "metadata.workplaces"); +}    function WorkplacesLoadingShell() {    return <InviteHubPageSkeleton titleNamespace="invites.workplaces" />;  }    function WorkplacesContent() {    const router = useRouter();    const searchParams = useSearchParams();    const t = useTranslations("invites.workplaces");    const tCommon = useTranslations("common");
